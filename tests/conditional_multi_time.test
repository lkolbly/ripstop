### CODE

module mux(bit a, bit b, bit sel) -> (bit c) {
    if sel[t-1] {
       c[t] = b[t-1];
    } else {
       c[t-1] = a[t-1];
    }
}

### OUTPUT
thread 'main' panicked at 'A given variable must be assigned at the same time step across conditional branches', ripstop/src/ir.rs:553:29
note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace
