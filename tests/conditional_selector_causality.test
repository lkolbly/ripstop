### CODE

module mux(bit a, bit b, bit sel) -> (bit c) {
    if sel[t] {
        c[t-1] = b[t-1];
    } else {
        c[t-1] = a[t-1];
    }
}

### OUTPUT
Expression depends on the future on line 3 col 8:
    if sel[t] {
       ^

Variable 'c' not assigned
