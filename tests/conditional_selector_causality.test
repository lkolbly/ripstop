### CODE

module mux(bit a, bit b, bit sel) -> (bit c) {
    if sel[t] {
        c[t-1] = b[t-1];
    } else {
        c[t-1] = a[t-1];
    }
}

### OUTPUT
thread 'main' panicked at 'Expression depends on the future!', ripstop/src/ir.rs:414:25
note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace
