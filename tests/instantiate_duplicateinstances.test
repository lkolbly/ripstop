### CODE

module shifter(bits<4> a) -> (bits<4> b) {
    b[t] = a[t][2:0] ++ 1'd0;
}

module instantiation(bits<4> a) -> (bits<4> b) {
    instantiate shifter as my_shifter;
    instantiate shifter as my_shifter;
    my_shifter.a[t] = a[t];
    b[t] = my_shifter.b[t];
}

### OUTPUT
thread 'main' panicked at 'Cannot have multiple instantiations with name my_shifter!', ripstop/src/ir.rs:737:17
note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace
